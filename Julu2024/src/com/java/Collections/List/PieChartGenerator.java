package com.java.Collections.List;



public class PieChartGenerator{

   /* public PieChartGenerator(String title) {
        super(title);

        // Create dataset
        DefaultPieDataset dataset = createDataset();

        // Create chart
        JFreeChart chart = ChartFactory.createPieChart(
                "Market Share 2024",  // Chart title
                dataset,              // Dataset
                true,                 // Include legend
                true,
                false
        );

        // Create ChartPanel and add it to the frame
        ChartPanel chartPanel = new ChartPanel(chart);
        chartPanel.setPreferredSize(new java.awt.Dimension(560, 370));
        setContentPane(chartPanel);
    }

    private DefaultPieDataset createDataset() {
        DefaultPieDataset dataset = new DefaultPieDataset();
        dataset.setValue("Brand A", 40);
        dataset.setValue("Brand B", 30);
        dataset.setValue("Brand C", 20);
        dataset.setValue("Brand D", 10);
        return dataset;
    }

    public static void main(String[] args) {
        PieChartGenerator chart = new PieChartGenerator("Pie Chart Example");
        chart.pack();
        chart.setVisible(true);
    }*/
}
